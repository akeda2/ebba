defaults:
  queue-mode: parallel
jobs:
- name: ebba-cleanup
  active: true
  path: ~/dev/ebba
  run-mode: scheduled
  build: ~/.cargo/bin/cargo clean
  at: "05:00"
- name: ebba
  active: true
  path: ~/dev/ebba
  test: cargo test --all --quiet
  build: 
    - ~/.cargo/bin/cargo build --release --quiet
    - ~/.cargo/bin/cargo install --path . --quiet
  post-build:
    - which gb && gb -I ebba.yaml -a -w
  interval: 3600
